# ── WindSense AI Backend — Python dependencies ──────────────────────
# Install with:  pip install -r requirements.txt

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # file uploads (CSV)

# Settings / validation
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0

# Database (PostgreSQL + portable SQLite fallback)
SQLAlchemy>=2.0.35
psycopg2-binary>=2.9.9         # PostgreSQL driver (Supabase / managed PG)

# Data + ML  (Python 3.13 / numpy 2.x compatible)
pandas>=2.2
numpy>=2.0
scikit-learn>=1.5
xgboost>=2.1
shap>=0.46
joblib>=1.4

# PDF generation (Asset Health Certificate) — pure Python, no system deps
fpdf2>=2.8

# Auth (JWT verification for Supabase tokens — ES256/RS256 via JWKS + HS256)
pyjwt>=2.9
cryptography>=42.0

# Misc
python-dateutil>=2.9
